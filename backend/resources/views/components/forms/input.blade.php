
<div>
    <label class="form-label">{{ $label }}</label>
    <input type="{{ $type }}" value="{{ $value }}" name="{{ $name }}" class="{{ $class }}" id="{{ $id }}" placeholder="{{ $placeholder }}">
</div>